server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Redirect root to /index.html
    location = / {
        return 301 /index.html;
    }

    # Serve static files
    location / {
        try_files $uri $uri/ =404;
    }
}
